# OmicsWeft
Building a package that ingests aligned multiomics data, runs every major integration method through one interface, and evaluates them. The package's value is breadth + reproducibility, since no single method wins across datasets.
